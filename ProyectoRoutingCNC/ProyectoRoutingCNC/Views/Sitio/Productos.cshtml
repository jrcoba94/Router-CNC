@model IEnumerable<Servicios.Model.Producto>
@{
    Layout = "~/Views/Shared/_Principal.cshtml";
}
<div class="products">
    <div class="container">
        <h3><span>Signos Zodiacales</span></h3>
        <div class="products-top">
            @foreach (var item in Model)
            {
                @*<div class="container">
                    <div class="row">*@
                        <div id="imageContent" class="col-md-3 prod-1">
                            <image src="~/@item.ImagenPortada" width="200" height="200"></image>
                        </div>
                        @*<div class="col-md-3">
                            @item.Nombre
                        </div>
                        <div class="col-md-3">
                            @item.Descripcion
                        </div>
                    </div>
                </div>*@
            }
            <div class="clearfix"> </div>
        </div>
    </div>
</div>

@*<script>
        $(document).ready(function () {
            $.ajax({
                async: true,
                type: 'POST',
                url: '@Url.Action("get_Productos", "Sitio")',
                success: function (data) {
                    $("#imageContent").html(data).append("<br/>");
                    var images = data.split("|||");
                    for (var i = 0, j = images.length; i < j; i++) {
                        $("#imageContent").append("<img src='" + images[i] + "' width='300'>");
                    }
                }
            });
        });
    </script>*@
